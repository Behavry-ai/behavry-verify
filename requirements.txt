# Runtime dependencies for the hosted service (Vercel reads this file).
# The offline CLI needs only `cryptography`; see pyproject.toml.
cryptography>=42.0
fastapi>=0.115
python-multipart>=0.0.9
httpx>=0.27
